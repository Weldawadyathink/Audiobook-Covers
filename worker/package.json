{
  "private": true,
  "version": "0.2.0",
  "name": "audiobookcovers-api",
  "description": "Run a audiobook cover retrieval api on the edge.",
  "main": "build/index.js",
  "scripts": {
    "build": "worktop build index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Fauna Labs <https://github.com/fauna-labs/>",
  "license": "MIT-0",
  "dependencies": {
    "@worker-tools/shed": "^0.1.0-pre.10",
    "worktop": "^0.7.3"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "worktop.build": "^0.0.4"
  }
}
